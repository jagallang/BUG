rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // v2.119.0: 임시로 모든 인증된 사용자에게 업로드 허용
    // Firestore 조회 방식이 503 에러를 유발하므로 단순화
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
